<?xml version="1.0" encoding="UTF-8"?>

<!--
This is only an example.
Do not copy real credentials into a file committed to Git.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="
              http://maven.apache.org/SETTINGS/1.2.0
              https://maven.apache.org/xsd/settings-1.2.0.xsd">

    <!-- Optional custom local repository -->
    <!--
    <localRepository>/custom/path/to/repository</localRepository>
    -->

    <mirrors>
        <!-- Example company mirror -->
        <!--
        <mirror>
            <id>company-mirror</id>
            <url>https://nexus.example.com/repository/maven-public/</url>
            <mirrorOf>*</mirrorOf>
        </mirror>
        -->
    </mirrors>

    <servers>
        <!-- Credentials belong here, not in pom.xml -->
        <!--
        <server>
            <id>company-repository</id>
            <username>${env.MAVEN_REPO_USER}</username>
            <password>${env.MAVEN_REPO_PASSWORD}</password>
        </server>
        -->
    </servers>

</settings>
